package com.armyknife.tools.data.storage

/**
 * DataStore preferences for user settings and favorites.
 */
object UserPreferences {
    const val KEY_FAVORITES = "favorites"
    const val KEY_DARK_THEME = "dark_theme"
}
